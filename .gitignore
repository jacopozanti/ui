node_modules
dist
*.log
.DS_Store
.claude/settings.local.json

*storybook.log
storybook-static

# This repo is npm-only (CI runs `npm ci`). A stray `pnpm`/`yarn` install would
# leave a second, silently drifting lockfile.
pnpm-lock.yaml
pnpm-workspace.yaml
yarn.lock
